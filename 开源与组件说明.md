# env4php 参赛作品权利说明与开源代码、组件使用情况说明

本文档面向**比赛提交与合规自查**，分为两部分：（一）参赛作者须确认或签署的权利与合规声明**范本**；（二）基于当前仓库可识别的**第三方开源与组件使用清单**及技术说明。  
**重要**：第（一）部分须由**参赛作者本人或团队**据实填写、核对并签署；第（二）部分为技术事实与引用关系说明。**本文档不构成法律意见**。

---

## （一）参赛作品权利与合规声明（范本）

> 请将下列声明复制到报名表、承诺书或单独签章页中，将「下划线或括号」处改为你的真实信息；若比赛主办方另有固定模板，以主办方文本为准，可将本清单作为附件。

**声明人（团队名称）：**________________  
**作品名称：** env4php（或贵方对外名称）  
**日期：**____年____月____日  

声明人郑重承诺并确认：

1. **著作权与原创性**  
   参赛作品中的**核心程序逻辑、界面编排、业务设计与文档**为声明人独立开发或已依法取得合法授权，声明人对依比赛规则需要提交的部分享有相应的著作权或依法取得的使用权，能够独立、完整地说明作品来源与创作过程。

2. **遵守法律法规**  
   作品的内容与传播方式不违反中华人民共和国宪法、法律、法规及比赛章程；不含有法律、行政法规禁止的内容。

3. **尊重他人知识产权**  
   声明人已知悉并确认：本作品在开发过程中使用了 **Flutter / Dart SDK** 及 **pub.dev 上公开发布的开源库**（详见本文档第（二）部分），以及随应用分发的 **OpenSSL、7-Zip** 等第三方二进制，其许可条件以各官方许可证及本仓库内所附 `LICENSE.txt` / `License.txt` 为准。声明人已尽合理注意义务，在许可允许范围内使用上述组件，并保留或附带了依法应保留的许可与版权说明。

4. **商标与商业标识**  
   声明人确认：除上述开源组件自带的名称与标识外，本作品使用的软件名称、图标等（如有）不侵犯他人注册商标专用权或其它商业标识权益；若使用第三方商标仅为描述兼容环境（如 PHP、Nginx 等），已在合理、描述性使用范围内。

5. **字体**  
   本作品在界面中优先使用 **Windows 系统已安装的「微软雅黑 / Microsoft YaHei UI」** 作为显示字体，**未在仓库内嵌入或再分发该字体文件**；最终用户设备上的字体授权由用户与其操作系统厂商之间的协议约束。

6. **真实性**  
   上述陈述真实、准确；如有不实，声明人愿承担由此引起的一切法律责任与比赛后果。

**声明人签字：**________________  

---

## （二）开源代码与组件使用情况说明

### 1. 构建与运行平台

| 组件 | 说明 |
|------|------|
| **Flutter SDK** | 跨平台 UI 与构建工具链；本作品 UI 基于 **Material 3**。许可见 [Flutter 官方仓库 LICENSE](https://github.com/flutter/flutter/blob/master/LICENSE)。 |
| **Dart SDK** | 语言与 `pub` 包管理；与 Flutter 配套。 |

版本会随开发环境变化；生成依赖树时可执行 `flutter --version` 与 `dart pub deps`。曾用参考环境：**Dart 3.8.x**、**Flutter 3.32.x**（以实际机器为准）。

---

### 2. Dart / Flutter 依赖（`pubspec.yaml` + `pubspec.lock`）

下列**直接依赖**的版本以仓库内 **`pubspec.lock`** 为准（执行 `flutter pub get` 后可能更新；提交比赛材料前请重新核对并同步更新本表）。

| 包名 | 典型锁定版本（见 lock） | 在本作品中的用途 |
|------|-------------------------|------------------|
| [flutter](https://pub.dev/packages/flutter)（SDK） | 随 SDK | 应用框架：`MaterialApp`、`ThemeData`、路由、`Overlay` 应用内提示等。 |
| [cupertino_icons](https://pub.dev/packages/cupertino_icons) | 1.0.8 | Cupertino 风格图标字体。 |
| [window_manager](https://pub.dev/packages/window_manager) | 0.3.9 | 桌面窗口：初始化、尺寸、居中、隐藏系统标题栏、关闭前确认等。 |
| [shared_preferences](https://pub.dev/packages/shared_preferences) | 2.5.3 | 本地键值存储：服务状态、设置、工具与卸载流程等。 |
| [http](https://pub.dev/packages/http) | 1.6.0 | HTTP 下载与网络请求。 |
| [path_provider](https://pub.dev/packages/path_provider) | 2.1.5 | 应用支持目录、文档目录等系统路径。 |
| [path](https://pub.dev/packages/path) | 1.9.1 | 路径拼接与规范化。 |
| [file_picker](https://pub.dev/packages/file_picker) | 8.3.7 | 文件/目录选择（如 Nginx 相关配置页）。 |
| [crypto](https://pub.dev/packages/crypto) | 3.0.7 | 摘要算法（MD5、SHA 等），用于工具页。 |
| [uuid](https://pub.dev/packages/uuid) | 4.5.2 | UUID 生成（如生成器页）。 |
| [flutter_local_notifications](https://pub.dev/packages/flutter_local_notifications) | 17.2.4 | **已声明依赖**；当前 **`lib/` 源码未引用**，通知由 `NotificationService` 通过 `Overlay` 实现。若需精简依赖，可在确认无用后从 `pubspec.yaml` 移除并重新 `pub get`。 |

**开发依赖：** `flutter_test`（SDK）、`flutter_lints`（如 5.0.0），用于测试与静态分析。

**传递依赖：** 上述库会引入 `ffi`、`win32`、`plugin_platform_interface` 等子依赖。**完整列表以 `pubspec.lock` 为准**，可用命令 `dart pub deps` 导出树形结构备查。各包具体许可证以 [pub.dev](https://pub.dev) 对应版本页及包内 `LICENSE` 文件为准。

**附：** 已另整理带 **URL、许可协议、用途** 的逐条表格，见同目录 **[开源组件清单.md](./开源组件清单.md)**。

---

### 3. 随作品分发的第三方二进制及许可文件

运行时会调用 **`lib/assets`** 下的工具（发布安装包时请与可执行文件一并包含许可文件）。

| 组件 | 仓库内许可文件 | 用途摘要 |
|------|----------------|----------|
| **OpenSSL**（如 `openssl-3.0.18.exe`） | `lib/assets/openssl/LICENSE.txt` | 调用命令行完成证书/密钥相关操作。**Apache License 2.0**（以该文件全文为准）。 |
| **7-Zip Extra**（如 `7za_2501.exe`） | `lib/assets/7z/License.txt` | 解压安装包、**XXH64** 校验等。**GNU LGPL** 为主，部分代码为 **BSD 2/3-Clause**（以该文件全文为准）。 |

使用 LGPL 类组件时，若比赛或发版要求提供**对应版本的源码获取方式或修改说明**，请遵循 **7-Zip 官方**与该许可证的现行要求（通常需保留许可文本、不错误署名）。

---

### 4. UI、系统字体与图标资源

| 项 | 说明 |
|----|------|
| **Material Design / Material Icons** | `pubspec.yaml` 中 `uses-material-design: true`；使用 Flutter 自带的 Material 图标字体。 |
| **Cupertino Icons** | 见依赖 `cupertino_icons`。 |
| **Windows 应用图标** | `windows/runner/resources/app_icon.ico` 为 Flutter 模板默认图标路径；若已替换为自制图标，请确保对该图标享有权利。 |
| **微软雅黑 / Microsoft YaHei UI** | 仅在主题中指定 `fontFamily`，**依赖用户系统已安装字体**，本仓库**不包含**字体二进制。 |

业务代码会从**应用可执行文件目录下的 `assets/icons`** 按名称加载 `php.png`、`{cate4}.png` 等图标（见 `lib/services/icon_service.dart`）。若你在安装包或发行介质中附带此类 PNG，且图案为他人商标或美术作品，**须自行确保已取得展示授权**或改用自有/开放素材。

---

### 5. 提交比赛前的自查清单（建议逐项勾选）

- [ ] 已阅读比赛章程中关于**原创性、开源使用、商业软件**的条款。  
- [ ] 已核对 **`pubspec.lock`** 与本文档版本表一致（或已更新文档）。  
- [ ] 已确认 **OpenSSL / 7-Zip** 的版本、来源与附带许可文件完整。  
- [ ] 已确认 **应用图标**、若有嵌入的**图片/音频/字体**均有合法来源。  
- [ ] 若作品名称、界面文案涉及**他人商标**，已评估为描述性使用或已取得授权。  
- [ ] 已将本文档（或节选）作为**附件**随作品说明一并提交（若赛方要求）。

---

### 6. 文档维护与免责

1. 依赖或资源变更后，请重新执行 `flutter pub get`，并更新 **`pubspec.lock`** 与本说明中的版本与描述。  
2. 本文档第（二）部分依据**当前仓库可见材料**整理；**不替代**律师或主办方对合规的认定。  

如有赛方指定格式（Word/PDF 固定页），可将「（一）声明范本」与「（二）组件表」复制到指定模板中提交。
